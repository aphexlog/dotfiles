# Global Skills

Put personal, reusable OpenCode skills in this directory. Each skill belongs in
its own directory with a `SKILL.md` file. This directory is Stow-linked to
`~/.config/opencode/skills` and is shared across projects.

Skills installed by the shared skills CLI remain in `~/.agents/skills`.
