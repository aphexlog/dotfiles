# Global Agents

Put personal, reusable OpenCode agents in this directory. It is Stow-linked to
`~/.config/opencode/agents` and is shared across projects.

Keep project-specific agents in a project's `.opencode/agents` directory.
